export const DEFAULT_ACTIVATE_AFTER_SECONDS = 1800 as const;

/**
 * The package name as a filename safe slug
 */
export const PACKAGE_NAME_SLUG = "dead-man-commit" as const;
export const PACKAGE_CONFIG_FILENAME = "config.yml" as const;
